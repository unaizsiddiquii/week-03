package day01linkedlist.doublylinkedlist.undoredofunctionalityfortexteditor;
// Class with name TextState and its purpose
// represents a state of the text content
public class TextState {
    String textContent;

    // Constructor to create a new text state
    public TextState(String textContent) {
        this.textContent = textContent;
    }
}
